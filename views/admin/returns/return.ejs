<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Returns</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>

    <link rel="stylesheet" href="/style/admin/returns/return.css">
  </head>
  <body class="min-h-screen bg-black text-white">
    <!-- Header Partial -->
    <%- include("../../partials/admin/admin-header") %>

    <div class="flex">
      <!-- Sidebar Partial -->
      <%- include("../../partials/admin/admin-sidebar") %>

      <!-- Page Content -->
      <main class="flex-1 p-6">
        <!-- Heading + Export Button -->
        <div class="flex justify-between items-center mb-4">
          <h1 class="text-2xl font-bold">Return Management</h1>
          <button class="bg-yellow-600 px-4 py-2 rounded">
            + Export Returns
          </button>
        </div>

        <!-- Counts -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
          <div class="count-box">
            <p class="text-gray-400">Total Returns</p>
            <p class="text-xl font-bold"><%= totalReturns %></p>
          </div>
          <div class="count-box">
            <p class="text-gray-400">Requested Returns</p>
            <p class="text-xl font-bold text-yellow-400">
              <%= requestedReturnsCount %>
            </p>
          </div>
          <div class="count-box">
            <p class="text-gray-400">Approved Returns</p>
            <p class="text-xl font-bold text-green-400">
              <%= approvedReturnsCount %>
            </p>
          </div>
          <div class="count-box">
            <p class="text-gray-400">Total Refund Amount</p>
            <p class="text-xl font-bold">
              Rs.<%= totalRefundAmount[0].totalRefund %>
            </p>
          </div>
        </div>

        <!-- Filter + Search -->
        <div class="flex justify-between mb-4">
          <span>All Returns</span>
          <div class="flex space-x-4">
            <select id="statusFilter" class="bg-gray-800 p-2 rounded">
              <option value="all">All Status</option>
              <option value="requested">Requested</option>
              <option value="approved">Approved</option>
              <option value="rejected">Rejected</option>
            </select>
            <div class="flex space-x-2">
              <input
                type="text"
                id="searchInput"
                placeholder="Search Return ID or Order ID or Customer..."
                class="bg-gray-800 p-2 rounded"
              />
              <button
                id="clearSearch"
                class="bg-gray-600 px-3 py-2 rounded hover:bg-gray-700"
                title="Clear Search"
              >
                <i data-lucide="x" class="w-5 h-5"></i>
              </button>
            </div>
          </div>
        </div>

        <!-- Returns Table -->
        <div class="overflow-x-auto">
          <table class="min-w-full table-auto border-collapse">
            <thead>
              <tr class="bg-gray-800">
                <th class="px-4 py-2 text-left">RETURN ID</th>
                <th class="px-4 py-2 text-left">ORDER ID</th>
                <th class="px-4 py-2 text-left">CUSTOMER</th>
                <th class="px-4 py-2 text-left">DATE</th>
                <th class="px-4 py-2 text-left">REFUND AMOUNT</th>
                <th class="px-4 py-2 text-left">ITEM</th>
                <th class="px-4 py-2 text-left">QUANTITY</th>
                <th class="px-4 py-2 text-left">STATUS</th>
              </tr>
            </thead>
            <tbody>
              <% returns.forEach(returnItem => { %>
              <tr class="border-b border-gray-700 hover:bg-gray-900 cursor-pointer" onclick="window.location.href='/admin/return/<%= returnItem.order_id %>?returnId=<%= returnItem._id %>'">
                <td class="px-4 py-2"><%= returnItem.returnID %></td>
                <td class="px-4 py-2"><%= returnItem.orderID %></td>
                <td class="px-4 py-2"><%= returnItem.address.fullName %></td>
                <td class="px-4 py-2"><%= new Date(returnItem.returnedAt).toLocaleDateString() %></td>
                <td class="px-4 py-2">Rs <%= returnItem.refundAmount %></td>
                <td class="px-4 py-2 align-top">
                  <%= returnItem.name %> <%= returnItem.mlSize ? `(${returnItem.mlSize}ml)` : '' %>
                </td>
                <td class="px-4 py-2 align-top">
                  <%= returnItem.quantity %>
                </td>
                <td class="px-4 py-2">
                  <% if(returnItem.adminApproved === 'Approved') { %>
                  <span class="status-approved">Approved</span>
                  <% } else if(returnItem.adminApproved === 'Rejected') { %>
                  <span class="status-rejected">Rejected</span>
                  <% } else { %>
                  <span class="status-requested">Requested</span>
                  <% } %>
                </td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>

        <!-- Pagination and Page Limit -->
        <div class="flex justify-between items-center mt-4">
          <!-- Pagination -->
          <div class="flex-1 flex justify-center items-center space-x-4">
            <% if (totalPages > 1) { %>
              <% if (currentPage > 1) { %>
                <a
                  href="/admin/returns?page=<%= currentPage - 1 %>&limit=<%= limit %>"
                  class="px-3 py-1 rounded bg-gray-800 text-white hover:bg-gray-700"
                >
                  <i data-lucide="chevron-left" class="w-5 h-5"></i>
                </a>
              <% } %>
              <span class="text-gray-400"><%= currentPage %> / <%= totalPages %></span>
              <% if (currentPage < totalPages) { %>
                <a
                  href="/admin/returns?page=<%= currentPage + 1 %>&limit=<%= limit %>"
                  class="px-3 py-1 rounded bg-gray-800 text-white hover:bg-gray-700"
                >
                  <i data-lucide="chevron-right" class="w-5 h-5"></i>
                </a>
              <% } %>
            <% } %>
          </div>

          <!-- Page Limit -->
          <div class="flex items-center space-x-2">
            <label for="pageLimit" class="text-gray-400">Show</label>
            <select id="limitSelect" onchange="changePageLimit(this.value)">
              <option value="5" <%= limit == 5 ? "selected" : "" %>>5</option>
              <option value="10" <%= limit == 10 ? "selected" : "" %>>10</option>
              <option value="20" <%= limit == 20 ? "selected" : "" %>>20</option>
              <option value="50" <%= limit == 50 ? "selected" : "" %>>50</option>
            </select>
            <span class="text-gray-400">per page</span>
          </div>
        </div>
      </main>
    </div>

    <!-- Error Popup Modal -->
    <div
      id="errorPopup"
      class="hidden fixed top-5 right-5 bg-red-600 text-white px-4 py-3 rounded-lg shadow-lg flex items-center space-x-3 z-50 transition-all duration-300"
    >
      <i data-lucide="alert-circle" class="w-5 h-5"></i>
      <span id="errorMessage">Something went wrong!</span>
      <button id="closeErrorPopup" class="ml-3 hover:text-gray-300">
        <i data-lucide="x" class="w-4 h-4"></i>
      </button>
    </div>

    <!-- Success Popup Modal -->
    <div
      id="successPopup"
      class="hidden fixed top-5 right-5 bg-green-600 text-white px-4 py-3 rounded-lg shadow-lg flex items-center space-x-3 z-50 transition-all duration-300"
    >
      <i data-lucide="check-circle" class="w-5 h-5"></i>
      <span id="successMessage">Success!</span>
      <button id="closeSuccessPopup" class="ml-3 hover:text-gray-300">
        <i data-lucide="x" class="w-4 h-4"></i>
      </button>
    </div>

    <!-- JS for actions -->
    <script>
      lucide.createIcons();

      // Handle page limit change
      function changePageLimit(limit) {
        const url = new URL(window.location.href);
        url.searchParams.set("limit", limit);
        url.searchParams.set("page", 1);
        window.location.href = url.toString();
      }

      // Function to filter table rows based on search and status
      function filterTable() {
        const searchQuery = document.getElementById("searchInput").value.trim().toLowerCase();
        const statusFilter = document.getElementById("statusFilter").value;
        const rows = document.querySelectorAll("tbody tr");

        rows.forEach((row) => {
          const returnId = row.querySelector("td:nth-child(1)").textContent.toLowerCase();
          const orderId = row.querySelector("td:nth-child(2)").textContent.toLowerCase();
          const customerName = row.querySelector("td:nth-child(3)").textContent.toLowerCase();
          const status = row
            .querySelector("td:nth-child(8) span")
            .textContent.toLowerCase();

          const matchesSearch = searchQuery === "" || returnId.includes(searchQuery) || orderId.includes(searchQuery) || customerName.includes(searchQuery);
          const matchesStatus =
            statusFilter === "all" ||
            (statusFilter === "requested" && status === "requested") ||
            (statusFilter === "approved" && status === "approved") ||
            (statusFilter === "rejected" && status === "rejected");

          row.style.display = matchesSearch && matchesStatus ? "" : "none";
        });
      }

      // Search functionality (triggered by Enter key)
      document.getElementById("searchInput").addEventListener("keypress", function (event) {
        if (event.key === "Enter") {
          event.preventDefault();
          filterTable();
        }
      });

      // Clear search
      document.getElementById("clearSearch").addEventListener("click", () => {
        document.getElementById("searchInput").value = "";
        filterTable();
      });

      // Status filter
      document.getElementById("statusFilter").addEventListener("change", filterTable);

      // Function to show error popup
      function showErrorPopup(message) {
        const popup = document.getElementById("errorPopup");
        const messageElement = document.getElementById("errorMessage");

        messageElement.textContent = message || "An unexpected error occurred.";
        popup.classList.remove("hidden");
        popup.classList.add("opacity-100", "translate-y-0");

        // Auto-hide after 4 seconds
        setTimeout(() => {
          popup.classList.add("hidden");
        }, 4000);
      }

      // Close manually
      document.getElementById("closeErrorPopup").addEventListener("click", () => {
        document.getElementById("errorPopup").classList.add("hidden");
      });

      // Function to show success popup
      function showSuccessPopup(message) {
        const popup = document.getElementById("successPopup");
        const messageElement = document.getElementById("successMessage");

        messageElement.textContent = message || "Success!";
        popup.classList.remove("hidden");
        popup.classList.add("opacity-100", "translate-y-0");

        // Auto-hide after 4 seconds
        setTimeout(() => {
          popup.classList.add("hidden");
        }, 4000);
      }

      // Close success popup manually
      document.getElementById("closeSuccessPopup").addEventListener("click", () => {
        document.getElementById("successPopup").classList.add("hidden");
      });

      // Display server-side flash messages on load
      <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
        showErrorPopup("<%= errorMessage %>");
      <% } %>
      <% if (typeof successMessage !== 'undefined' && successMessage) { %>
        showSuccessPopup("<%= successMessage %>");
      <% } %>
    </script>
  </body>
</html>